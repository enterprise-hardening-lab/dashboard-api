name: 🧩 App Container Governance

on:
  push:
    branches: [ main ]
  workflow_dispatch:

# 👇 Top-level permissions explicitly grant reusable workflow inheritance
permissions:
  contents: read
  id-token: write
  actions: read

jobs:
  orchestrate:
    name: 🧩 Invoke Container Hardening Orchestrator
    uses: enterprise-hardening-lab/container-hardening-orchestrator/.github/workflows/orchestrator.yml@main
    with:
      app_name: "dashboard-api"
      base_image: "ubuntu"
    permissions: inherit
    secrets: inherit
