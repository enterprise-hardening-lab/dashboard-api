name: 🧩 App Container Governance

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  id-token: write        # ✅ Required for GitHub OIDC → AWS authentication
  contents: read         # ✅ Allows workflow to read contents from the repo

jobs:
  orchestrate:
    uses: enterprise-hardening-lab/container-hardening-orchestrator/.github/workflows/orchestrator.yml@main
    with:
      app_name: "dashboard-api"
      base_image: "ubuntu"
    secrets: inherit      # ✅ Forwards all secrets (if needed)
